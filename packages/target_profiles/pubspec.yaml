name: shooting_companion_target_profiles
description: Versioned official and custom target definitions.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  shooting_companion_domain:
    path: ../domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
