name: shooting_companion_vision_api
description: Immutable JSON contracts for experimental offline target analysis.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  shooting_companion_domain:
    path: ../domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
