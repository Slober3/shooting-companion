name: shooting_companion_training
description: Versioned offline drills, controlled experiments and sight corrections.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.2

dependencies:
  shooting_companion_domain:
    path: ../domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
